# Third-Party Notices

This repository imports third-party packages but does not copy or redistribute their source code.

Runtime dependencies:

- `pypdf` — BSD-3-Clause
- `python-docx` — MIT
- `python-dotenv` — BSD-3-Clause

Development dependency:

- `pytest` — MIT

Ollama and all downloaded model weights are separate software or model artifacts with their own terms. A model being available through Ollama does not mean it uses the same license as Ollama. Review the model-specific license before use, redistribution, or commercial deployment.

This notice is informational and is not legal advice. Recheck dependency and model terms when changing versions or models.
